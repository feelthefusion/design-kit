> **Design Kit overlay.** Mobile speed is judged at a 375px phone on a throttled connection first; desktop scores do not count as proof. Fix, re-measure, repeat until LCP ≤ 2.5s, INP ≤ 200ms and CLS ≤ 0.1. Fonts are a CLS source: follow **type-rendering** (metric-matched fallbacks, preload only the primary faces). The Design Kit fetches only this skill from addyosmani/web-quality-skills; linked references open upstream on GitHub (read them with `web_extract` on Hermes, WebFetch on Claude Code).
