## Design Kit notes

- Motion tokens (durations, springs, easing) and the reduced-motion policy belong in `docs/DESIGN.md` (see **design-direction**). Use them instead of inventing per-component values.
- Text rendering (smoothing, optical sizing, tracking by size, fallback metrics) is detailed in **type-rendering**. The gate enforces the mechanical parts (`type.*` findings).
